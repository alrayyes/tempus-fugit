# Analytics are opt-in. Leave these unset for a local checkout or a fork - the
# site builds and runs fine with zero tracking. Production supplies the real
# values as GitHub Actions repository variables at build time; they are never
# committed.

PUBLIC_MATOMO_URL=
PUBLIC_MATOMO_SITE_ID=
PUBLIC_UMAMI_SCRIPT_URL=
PUBLIC_UMAMI_WEBSITE_ID=
